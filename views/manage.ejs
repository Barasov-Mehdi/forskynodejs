<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Resim Yönetimi</title>
    <style>
        body {
            background: #121212;
            color: #f5f5f5;
            font-family: Arial, sans-serif;
            padding: 20px;
        }

        h1 {
            text-align: center;
            color: #00ff7f;
            margin-bottom: 20px;
        }

        .menu {
            text-align: center;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }

        .menu a {
            background: #1e1e1e;
            color: #00ff7f;
            padding: 10px 15px;
            border-radius: 8px;
            text-decoration: none;
            margin: 5px;
            display: inline-block;
            transition: 0.3s;
        }

        .menu a:hover {
            background: #00ff7f;
            color: #121212;
        }

        /* Arama kutusu */
        .search-box {
            text-align: center;
            margin-bottom: 30px;
        }

        .search-box input {
            padding: 10px;
            width: 250px;
            max-width: 80%;
            border: none;
            border-radius: 8px;
            outline: none;
            background: #1e1e1e;
            color: #00ff7f;
            font-size: 1rem;
            text-align: center;
        }

        .search-box button {
            padding: 10px 15px;
            margin-left: 8px;
            background: #00ff7f;
            color: #121212;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-weight: bold;
            transition: 0.3s;
        }

        .search-box button:hover {
            background: #00cc66;
        }

        /* Kart konteyner */
        .container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            justify-content: center;
        }

        .card {
            background: #1e1e1e;
            border-radius: 12px;
            padding: 15px;
            width: 250px;
            text-align: center;
            transition: 0.3s;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 10px rgba(0, 255, 127, 0.3);
        }

        img {
            width: 100%;
            height: 150px;
            object-fit: cover;
            border-radius: 8px;
        }

        .actions {
            margin-top: 10px;
        }

        .actions a {
            color: #00ff7f;
            margin-right: 10px;
            text-decoration: none;
        }

        .actions button {
            background: #ff4d4d;
            color: #fff;
            border: none;
            padding: 6px 10px;
            border-radius: 5px;
            cursor: pointer;
        }

        @media (max-width: 600px) {
            h1 {
                font-size: 1.8rem;
            }

            .card {
                width: 90%;
            }

            .search-box input {
                width: 70%;
            }
        }
    </style>
</head>

<body>
    <h1>Şəkil Paneli</h1>

    <div class="menu">
        <a href="/">Əsas Səhifə</a>
        <a href="/api/images/new">➕ Yeni Şəkil Yüklə</a>
        <a href="/api/images">📸 Galeriyaya Bax</a>
    </div>

    <div class="search-box">
        <form action="/api/images/search" method="GET">
            <input type="text" name="category" placeholder="Kategoriye göre ara..." required>
            <button type="submit">Ara</button>
        </form>
    </div>

    <div class="container">
        <% images.forEach(image=> { %>
            <div class="card">
                <img src="<%= image.imageUrl %>" alt="<%= image.title %>">
                <h3>
                    <%= image.title %> - <%= image.category %>
                </h3>
                <div class="actions">
                    <a href="/api/images/edit/<%= image._id %>">Düzenle</a>
                    <form action="/api/images/delete/<%= image._id %>" method="POST" style="display:inline;">
                        <button type="submit">Sil</button>
                    </form>
                </div>
            </div>
            <% }) %>
    </div>
</body>

</html>